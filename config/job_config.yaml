# This file defines a specific job for the crew to execute.
crew_name: "Document Crew"
usecase: "Document"

# Runtime parameters for this specific job
module_name: "document"
output_dir_path: "output/intermediate/document_crew" # Made it more specific for this run

# The 'inputs' dictionary will be passed to the crew's kickoff method.
inputs:
  topic: "A new AI-powered coffee machine"
  target_audience: "Tech enthusiasts and busy professionals"