crew:
  name: "Document Crew"
  usecase: "Document"
  description: "Crew for generating Functional document"

  agents:
    azure_usecase_explainer: azure_usecase_explainer  # references an existing agent config
    gpt_verifier: gpt_verifier                     # references an existing agent config
    gemini_writer: gemini_writer                     # references an existing agent config
  tasks:
    functionality_task: functionality_task            # references an existing task config
    introduction_task: introduction_task              # references an existing task config
    verify_task: verify_task                          # references an existing task config


